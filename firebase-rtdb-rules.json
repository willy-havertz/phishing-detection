{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "scans": {
          ".indexOn": ["created_at", "classification"]
        }
      }
    }
  }
}
